# Validation contract

This repository is reconstructed against a pinned numerical reference rather than by copying the legacy package wholesale.

## Legacy reference

```text
repository: ignaciomagana/darksirens
commit:     c042527238bd71421b792936bc48c3b815b90d6d
```

The first parity anchor is the legacy unified K=1 fixed-coordinate likelihood bank. Candidate implementations must reproduce the recorded values before the corresponding mature behavior is considered migrated.

No reference file in `tests/reference/legacy/` may be regenerated from the reconstructed implementation. Reference updates require an explicit new legacy reference commit and provenance update.
