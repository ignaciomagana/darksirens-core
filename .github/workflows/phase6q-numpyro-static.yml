name: phase6q-numpyro-static

on:
  push:
    branches: [rebuild/phase6-inference-io]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  numpyro-static:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
    steps:
      - name: Check out candidate
        uses: actions/checkout@v4
        with:
          path: candidate

      - name: Check out pinned legacy reference
        uses: actions/checkout@v4
        with:
          repository: ignaciomagana/darksirens
          ref: c042527238bd71421b792936bc48c3b815b90d6d
          path: legacy

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependency-minimal JAX test tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            "numpy==1.26.4" \
            "jax==0.4.38" \
            "jaxlib==0.4.38" \
            "pytest==8.3.4" \
            "ruff==0.15.20"
          python -m pip install --no-deps -e candidate

      - name: Definite-error lint
        working-directory: candidate
        run: |
          ruff check src/darksirens/inference/numpyro_static.py tests/test_numpyro_static.py tools/probe_numpyro_static.py --select F,E9
          python -m compileall -q src/darksirens/inference/numpyro_static.py tests/test_numpyro_static.py tools/probe_numpyro_static.py

      - name: Verify NumPyro and JAX stay lazy on module import
        working-directory: candidate
        run: |
          python - <<'PY'
          import sys
          import darksirens.inference.numpyro_static as static

          assert callable(static.prepare_numpyro_static_plan)
          assert "jax" not in sys.modules
          assert "jax.numpy" not in sys.modules
          assert "numpyro" not in sys.modules
          print("6Q module import loads neither JAX nor NumPyro")
          PY

      - name: Focused Phase 6Q tests
        working-directory: candidate
        run: python -m pytest -q tests/test_numpyro_static.py

      - name: Evaluate frozen legacy static NumPyro behavior
        run: |
          PYTHONPATH="$GITHUB_WORKSPACE/legacy" python candidate/tools/probe_numpyro_static.py \
            --implementation legacy \
            --legacy-root "$GITHUB_WORKSPACE/legacy" \
            --out /tmp/numpyro-static-legacy.json

      - name: Evaluate reconstructed static NumPyro behavior
        run: |
          PYTHONPATH="$GITHUB_WORKSPACE/candidate/src" python candidate/tools/probe_numpyro_static.py \
            --implementation candidate \
            --out /tmp/numpyro-static-candidate.json

      - name: Require exact Phase 6Q parity
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          legacy = json.loads(Path('/tmp/numpyro-static-legacy.json').read_text())
          candidate = json.loads(Path('/tmp/numpyro-static-candidate.json').read_text())
          if legacy != candidate:
              raise SystemExit(
                  "NumPyro static-contract parity failure:\n"
                  f"legacy={legacy!r}\n"
                  f"candidate={candidate!r}"
              )
          print("Phase 6Q frozen/new static NumPyro behavior is exact")
          PY
